{
  "name": "shoply",
  "version": "1.0.0",
  "description": "Shoply - Fullstack Multi-Purpose E-commerce Platform with Telegram Bot and Standalone Admin",
  "scripts": {
    "install:all": "npm install && cd server && npm install && cd ../client && npm install && cd ../admin && npm install",
    "server": "cd server && npm run dev",
    "client": "cd client && npm run dev",
    "admin": "cd admin && npm run dev",
    "dev": "concurrently -n \"SERVER,CLIENT,ADMIN\" -c \"blue,green,magenta\" \"npm run server\" \"npm run client\" \"npm run admin\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
